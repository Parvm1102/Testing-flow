# Inference-only dependencies for the Gridlock demo backend.
#
# Versions are PINNED to the exact training environment (Python 3.10) so the
# pickled artifacts in `models/` and `hotspot_artifacts/` unpickle without
# scikit-learn / xgboost / numpy version skew. Training-only libs (shap, seaborn,
# langdetect, imbalanced-learn) are excluded to keep the image lean; `optuna`
# stays because the pickled ensembles live in `src.models`, which imports it.
#
# `torch` is the CPU build (2.12.1+cpu). The Dockerfile installs it from the
# PyTorch CPU index FIRST, so the pin below is already satisfied (no CUDA pull).

# Web server
fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.13.4

# Core data stack
pandas==2.3.3
numpy==2.2.6
scipy==1.15.3
scikit-learn==1.7.2

# Gradient boosting (the stacked ensembles use all three)
lightgbm==4.6.0
xgboost==3.2.0
catboost==1.2.10

# Required to unpickle the src.models ensemble artifacts
optuna==4.9.0

# Multilingual sentence embeddings (Kannada + English). This sentence-transformers
# release requires torch>=2.4; 2.12.1 is the CPU build used during training.
sentence-transformers==5.6.0
transformers==5.12.1
tokenizers==0.22.2
huggingface_hub==1.20.1
torch==2.12.1

# Persistence / IO
joblib==1.5.3
pyarrow==24.0.0
